# The wordmark SVG is disabled (empty string, not deleted, so the E25DX theme
# default doesn't merge back in) — the header logo already carries the name.
svg: ""

badge: Written by Doc Holiday

# The <span> gets the gradient text treatment (see hero.css).
title: LangGraph, explained at the <span>concept level</span>.

description: A field guide to the LangGraph codebase — how the engine schedules nodes, what your state and topology compile into, why checkpoints are shaped the way they are, and the replay contract your code must honor. Written by exploring the source directly, page by page.

btn_primary:
  label: Read the guide
  url: docs/the-big-picture/

btn_secondary:
  label: LangGraph on GitHub
  url: https://github.com/langchain-ai/langgraph
